# Nota de Obsidian: `AgendaScreen`

## 📁 Ubicación en el Proyecto

`lib/presentation/screens/agenda_screen.dart`

Pantalla raíz de la pestaña "Agenda" dentro de `NavegacionPrincipalScreen`.

## 🎯 Propósito del Archivo

Placeholder: por ahora solo muestra el texto "Próximamente". La capa de datos ya está lista (`AgendaEventoModel`, `agenda_evento_repository.dart`, `agendaEventosProvider`) — falta construir la UI real de la agenda de eventos veterinarios, que se retoma como tarea aparte.

---

## ⚙️ Glosario de Funciones y Componentes Complejos

Ninguno todavía: es un `StatelessWidget` simple con un `Scaffold`, un `AppBar` con título y `MenuUsuarioAvatar` (ver `menuUsuarioAvatar.md`), y un texto centrado. Actualizar esta nota cuando se implemente la pantalla real.
