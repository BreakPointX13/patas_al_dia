# Nota de Obsidian: `MapaScreen`

## 📁 Ubicación en el Proyecto

`lib/presentation/screens/mapa_screen.dart`

Pantalla raíz de la pestaña "Mapa" dentro de `NavegacionPrincipalScreen`.

## 🎯 Propósito del Archivo

Placeholder: por ahora solo muestra el texto "Próximamente". Es el destino planeado para la feature de mascotas extraviadas (reporte + mapa), que el roadmap deja explícitamente fuera del alcance de la v1 — ver la sección "Roadmap — Segunda versión (v2)" en `CLAUDE.md`. La tabla `mascotas_extraviadas` ya existe en `DatabaseHelper`, pero todavía no tiene modelo, repository ni provider.

---

## ⚙️ Glosario de Funciones y Componentes Complejos

Ninguno todavía: es un `StatelessWidget` simple con un `Scaffold`, un `AppBar` con título y `MenuUsuarioAvatar` (ver `menuUsuarioAvatar.md`), y un texto centrado. Actualizar esta nota cuando se implemente la pantalla real.
